import { NumerologicalSequence } from './types';

// This function is now empty since we're removing the numerological sequences
export const drawNumerologicalSequence = (
  ctx: CanvasRenderingContext2D,
  seq: NumerologicalSequence
) => {
  // No longer drawing numerological sequences
};
